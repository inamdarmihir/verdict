"""Runnable examples for the Verdict library.

* ``worked_example`` — offline design-spec contrast (rename vs boundary)
* ``langgraph_supervisor`` — end-to-end LangGraph host-loop wiring

Install and run from the repository root::

    pip install -e .
    python examples/worked_example.py

    pip install -e ".[langgraph]"
    python examples/langgraph_supervisor.py
"""
